import { FastifyReply, FastifyRequest } from 'fastify';

export const handleUserRegister = (request: FastifyRequest, reply: FastifyReply) => {

}

export const handleUserLogin = (request: FastifyRequest, reply: FastifyReply) => {
    
}